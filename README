# Game Master Radio

# Requirements:
sudo apt-get install python-django #(1.1)
sudo apt-get install python-setuptools
sudo easy_install Werkzeug
sudo easy_install -Z django-admin-tools
sudo easy_install pil

# "sudo sh README" to install
